#include 'protheus.ch'
#include 'parmtype.ch'

User function CadTxAbate()

	Private cAlias  	:= "SZ8"
	Private cTitulo 	:= "Cadastro da tabela de taxas de abate"

	//Private aRotAdic 	:= {}
	//aadd(aRotAdic,{ "Copia Registro","U_CopiaReg()", 0 , 6 })	
	//AxCadastro(cAlias, cTitulo, "U_DelOk()", "U_COK()", aRotAdic, bPre, bOK, bTTS, bNoTTS, , , aButtons, , ) 
	
	AxCadastro(cAlias, cTitulo, , , , , , , , , , , , ) 
	
Return()
